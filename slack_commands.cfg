define command {
                command_name                          slack-service
                command_line                          /usr/lib64/nagios/plugins/Nagios2Slack/slack_service_notify "$NOTIFICATIONTYPE$"  "$HOSTNAME$" "$HOSTADDRESS$" "$SERVICEDESC$" "$SERVICESTATE$" "$SERVICEOUTPUT$" "$LONGDATETIME$"
}

define command {
                command_name                          slack-host
                command_line                          /usr/lib64/nagios/plugins/Nagios2Slack/slack_host_notify "$NOTIFICATIONTYPE$"  "$HOSTNAME$" "$HOSTADDRESS$" "$HOSTSTATE$" "$HOSTOUTPUT$" "$LONGDATETIME$"
}
